// Demand Forecast Module Stub
const DemandForecast = {
  getForecast(productId) {
    return { predicted: 0, confidence: 0 };
  },
  getSupplierForecast(supplierId) {
    return [];
  }
};

export default DemandForecast;
